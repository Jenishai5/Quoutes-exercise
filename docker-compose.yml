services:
  backend:
    build: ./backend
    restart: unless-stopped
    volumes:
      - quotes-data:/app/data
    expose:
      - "8000"
  frontend:
    build: ./frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "8080:80"
volumes:
  quotes-data:
